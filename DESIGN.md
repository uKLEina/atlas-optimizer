# Atlas Tree Optimizer 設計書

PoE Atlas Treeのポイント配分を最適化するツールの要件・設計決定事項のまとめ。
進捗状況と作業計画は `ROADMAP.md` を参照。

## 目的

ユーザが「配置したいノード」を指定すると、**それらを全て含み、rootに連結で、必要ポイント数が最小**となる配置を提案する。既存ツール(Path of Pathing)はヒューリスティックのため非最適解を出すことがある。本ツールは**数学的に最適性が証明された厳密解**を返すことを差別化点とする。

## 要件

### 機能要件

| 項目 | 決定 |
|---|---|
| 対象データ | `data.json` のみ(現行リーグ 3.29 用)。league.json は使わない |
| 実行環境 | ブラウザ完結(ローカル動作)。サーバー・デプロイは不要 |
| 厳密性 | 必須。最適性の証明付き解のみを「最適」と表示する |
| 処理時間 | 数秒まで許容(実測はサブ秒〜1.5秒) |
| ポイント上限 | 138(`data.json` の `points.totalPoints`) |

### UI要件

1. **任意ノードの指定**: Notable/Keystoneに限らず全ノード種を配置対象に指定できる
2. **除外設定**: 「このノードは取らない」指定が可能。クリックで「指定→除外→中立」の3状態トグル(Path of Pathing踏襲)
3. **除外により到達不能になったterminalは無視する**(Path of Pathing踏襲)。エラーにはしない
4. **Masteryクリック**: **同名Masteryの全クラスタ**のNotableを一括指定する
   (当初はPoP踏襲でグループ単位だったが、2026-08-12のユーザレビューで全域に変更)。
   対象Notableが無ければno-op。Masteryホバー時は同名Masteryノード(全クラスタ)を黄色で強調表示
5. **描画の例外**: Wormhole同士を結ぶ長距離辺は描画しない(ソルバーは通常辺として使う)
6. **PoE Plannerエクスポート**: 現在の配置をPoE PlannerのURLとして出力できる(形式は後述、実地検証済み)

## データに関する知見

- ノード1030個のうち126個は `isMastery`(グループ中央の飾り、辺なし)で最適化対象外
- 実質グラフ: **903ノード / 1003辺**、mastery除外後は全ノードがrootから到達可能
  (pyref/web 双方のテストが実測値で検証している)
- `in`/`out` はデータ上の向きがあるだけで、ゲーム的には無向グラフとして扱う
- **スタートノード29045**(名前空)はポイントを消費しない(コスト0)。root(擬似ノード)も同様
- Wormhole(Gateway 3ペア6ノード)は通常の辺としてデータに含まれており、特別扱い不要
- Keystone「Unwavering Vision」(id 65225)は取得すると+20ポイント(予算138→158)。
  **対応済み(2026-08-15)**: 予算表示を、解に含まれるボーナスノード分だけ動的に加算する
  (`Grants N Atlas Passive Skill Points` の stat から抽出、id 直書きしない)。
  取得するかどうかは通常ノードと同じくユーザのクリックで指定する。
  当初実装した「予算超過時に取得版の代替解を自動提示するアドバイザー」は
  **ユーザレビューで不採用**(使うケースがほぼ無く、UIの混乱を招くだけと判断)
- グラフの構造的性質: サイクル空間次元101(木+101辺)、treewidth ≤ 6
- ツリー描画用の座標は `groups` の x,y + `orbit`/`orbitIndex` の極座標、画像アセットは
  submodule の `assets/` にある。**16/40スロット軌道の角度は等間隔ではない**
  (30°/45°、10°/45°の混在。GGG公式 skilltree-export README と PoB 実装が典拠。
  等分割で計算すると大半のノードがゲーム内表示とズレる — 2026-08-15に実際にやった)

## 最適化アルゴリズム(検証済み)

問題は**ルート付き・ノード重みSteiner木問題**(通常ノード重み1、root/スタートノード重み0)。NP困難だが、このグラフ規模・構造なら厳密解が高速に得られる。

### 採用: 前処理縮約 + ILP(HiGHS)

1. **前処理**(terminal指定のたび、ミリ秒オーダー):
   - 除外ノードをグラフから削除 → rootから到達不能になったterminalを除外(無視リストとしてUIに通知)
   - 非terminalの葉を再帰的に刈り込み
   - 非terminalの次数2ノードをチェーンごと重み付き辺に縮約(内部ノード数 = 辺重み)
   - 平行辺は最小重みのみ残す
   - → 核グラフは100〜250ノード程度に潰れる
2. **ILP定式化**(縮約グラフ上、rootから各terminalへの単一品種フロー):
   - 変数: y_v(ノード選択、バイナリ)/ x_a(arc使用、バイナリ、チェーン内部コスト付き)/ f_a(フロー、連続)
   - 制約: フロー保存 / f ≤ K·x / x ≤ y(両端)/ 各ノード入次数 ≤ 1
   - ソルバー: ブラウザでは [highs-js](https://github.com/lovasoa/highs-js)(HiGHS の WASM ビルド)
3. **解の展開**: 使用された縮約辺を元のチェーンに展開して配置ノード集合を復元

### ベンチマーク(pyref リファレンス実装、`python -m atlasopt bench` で再現可能)

ランダムなNotable(シード42)をterminalとした場合。全ケース最適性証明付き:

| terminal数 | ポイント数 | 求解時間 |
|---|---|---|
| 10 | 81 | 0.6s |
| 20 | 120 | 0.9s |
| 30 | 145 | 0.7s |
| 40 | 197 | 0.7s |
| 50 | 229 | 0.7s |
| 60 | 262 | 0.4s |

### 不採用とした選択肢

- **縮約なしの素朴なフロー定式化**: big-M制約が緩くLP緩和が弱い。K=60で240秒でも最適性証明が完了しない(gap 1.5%)
- **Dreyfus-Wagner系DP**(3^k): terminal数15が実用限界。138ポイント級ビルドの指定数(40〜60)に耐えない
- **Treewidth DP**: tw≤6のため理論上は最速・実行時間決定的だが実装コストが高い。ILPで実用十分と実証されたため温存。ILPが遅い病的ケースが見つかった場合の切り札とする

## PoE PlannerエクスポートURL形式

poeplanner.com のJSバンドル解析により特定。生成URLが実際にPoE Plannerで正しく開けることを実地検証済み(スタートノード込みでポイント数表示も整合)。

```
URL = https://poeplanner.com/atlas-tree/ + base64(bytes)
      ただし base64 の '+'→'-', '/'→'_' 置換('=' パディングは残す)

bytes(全てリトルエンディアン):
  u16 serializationVersion = 5
  u16 treeVersion = 28          # 3.29.0 に対応
  u8  isPoE2 = 0
  u16 選択ノード数
  ノード数 × u16 ノードID       # GGGのskill id = data.jsonのキーそのまま
  u16 gzip長
  gzip(メモ文字列)              # 空文字で可。ただし有効なgzipストリームであること
```

- 選択ノードにはスタートノード29045を含めてよい(PoE Planner側でポイント0として扱われる)
- `treeVersion` はリーグ更新で変わる。`https://cdn.poeplanner.com/json/versions.json` の `atlasVersions` から取得できる(ハードコード+更新手順のドキュメント化でも可)

## 実装アーキテクチャ

2実装+照合オラクル方式を採る:

- **`pyref/`(Python、リファレンス実装)**: 仕様原本。正しさは4層の照合ピラミッド
  (全列挙 / Dreyfus-Wagner DP / 素朴ILP / 採用定式化の相互照合+乱数テスト)で担保。
  詳細は `pyref/README.md`
- **`web/`(TypeScript、プロダクション実装)**: ブラウザ完結の Web UI。
  ソルバーは highs-js。正しさは pyref とのランダムケース照合(期待解のJSON出力を
  TS側で全一致検証)で担保する

### フェーズ2 技術スタック(2026-08-12決定)

`web/` 以下に独立した npm パッケージとして作る。Node は v24 LTS(nvm 管理)。

| 道具 | 役割 | 採用理由 |
|---|---|---|
| Vite | 開発サーバー+ビルド | 設定ほぼゼロで TS+WASM が動く。事実上の標準 |
| TypeScript (strict) | 言語 | pyref 移植時の型安全。strict 必須 |
| Vitest | テストランナー | pytest 相当。Vite と設定を共有できる |
| highs-js | ILPソルバー(HiGHS の WASM) | 採用アルゴリズム(上述)の決定事項 |

**UIフレームワーク(React 等)は採用しない。** 本体は canvas 描画とソルバーで
フレームワークの恩恵が薄く、残りのUIは素のDOMで足りる規模のため。
必要になった時点での追加は妨げない。

## 同点最適解のタイブレーク(2026-08-15 実装)

同点最適解の中からユーザが直感的に望む経路を選ぶ。**ポイント最適性(厳密性)は不変**。

- 方式: **2フェーズの辞書式最適化**。
  フェーズ1は従来どおりポイント数のみを最小化(純整数目的、従来速度・従来証明)。
  フェーズ2は「Σ cost·y + Σ w·x = フェーズ1の最適値」を等式制約に加え、
  重み Σε のみを最小化する。重みは主目的に一切混ざらないため厳密性は構造的に不変。
  points は目的値ではなく |nodes| − 1 で数える
- フェーズ2はベストエフォート: フェーズ1解の ε 値をカットオフ制約にして枝刈りし、
  mip_rel_gap 2%・時間上限 15 秒。optimal に至らなければフェーズ1の経路のまま返す
- 表示はフェーズ2完了まで待つ(合計で無重み時の約2倍)。フェーズ1時点の暫定表示→
  経路差し替えのプログレッシブ方式も実装したが、**表示が置き換わる挙動が不評で
  ユーザレビューにより不採用**(2026-08-15)
- 縮約の平行辺の間引きは (w, ε) の**辞書式**比較
  (同一 w の平行チェーンの選択は縮約段階で決まるため、ε を見ないと選好が潰れる。
  和で比べると ε が大きい時に w の優劣を覆すので辞書式であること)
- 重み(整数ティア 0〜7、`web/src/solver/tiebreak.ts` が生成。小=優先):
  1. ユーザ指定ノードが属する Mastery グループのノード(0)
  2. Mastery グループ外: Mod Effect(1)→ Rarity(2)→ Quantity(3)→ Scarab(4)→ Maps(5)→ その他(6)
  3. 指定と無関係な Mastery グループのノード(7、最回避)
- 複数カテゴリの stat を持つノードは最上位カテゴリで分類する
- pyref 側は汎用の `node_weights` 引数のみ(選好ポリシーは UI 層の責務)。
  crosscheck は4ケースに1回、重み付きでもポイント一致を検証する

## 後回しと決定した項目

- league.json / ruthless 系データへの対応
